import { fetchPuzzleInput } from "../common";

export async function part1(): Promise<number> {
    const puzzleInput = await fetchPuzzleInput(2023, 1);
    console.log(`puzzleInput: ${puzzleInput}`);

    return 0;
}